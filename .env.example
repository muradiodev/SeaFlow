# Use a Neon PostgreSQL connection string. Never commit real credentials.
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require
# Generate a unique random value before running the application.
SESSION_SECRET=replace-with-a-long-random-secret
PORT=5000
HOST=127.0.0.1
